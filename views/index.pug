extends layout

block content
  h1= title
  p Welcome to #{title}

  form(method='post', action='/')
        input(type='text', name='user_name')
        input(type='submit', value='OK')

  p __dirnameは#{dirname}